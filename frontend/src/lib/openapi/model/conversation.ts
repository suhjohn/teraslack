/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * Teraslack API
 * Canonical HTTP API for Teraslack.
 * OpenAPI spec version: 1.0.0
 */
import type { ConversationType } from './conversationType';
import type { TopicPurpose } from './topicPurpose';

/**
 * A conversation — a channel, DM, or group DM where messages are exchanged.
 */
export interface Conversation {
  /** Unique conversation identifier. */
  id: string;
  /** Workspace this conversation belongs to. */
  workspace_id: string;
  /** Display name of the conversation (e.g. "general", "deploy-alerts"). */
  name: string;
  type: ConversationType;
  /** User who created this conversation. */
  creator_id: string;
  /** Whether the conversation has been archived. */
  is_archived: boolean;
  topic: TopicPurpose;
  purpose: TopicPurpose;
  /** Current number of members in the conversation. */
  num_members: number;
  /** Timestamp of the most recent message. */
  last_message_ts?: string;
  /** Timestamp of the most recent activity (message, reaction, etc.). */
  last_activity_ts?: string;
  /** Timestamp of the last message the authenticated user has read. */
  last_read_ts?: string;
  /** Whether the authenticated user has unread messages in this conversation. */
  has_unread?: boolean;
  created_at: string;
  updated_at: string;
}
