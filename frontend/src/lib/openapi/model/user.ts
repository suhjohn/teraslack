/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * Teraslack API
 * Canonical HTTP API for Teraslack.
 * OpenAPI spec version: 1.0.0
 */
import type { AccountType } from './accountType';
import type { DelegatedRole } from './delegatedRole';
import type { PrincipalType } from './principalType';
import type { UserProfile } from './userProfile';

/**
 * A user identity — can represent a human, an AI agent, or a system service.
 */
export interface User {
  /** Unique user identifier. */
  id: string;
  /** Workspace this user belongs to. */
  workspace_id: string;
  /** Canonical account identity for this workspace-local user when linked. */
  account_id?: string;
  /** Unique username within the workspace (used for @-mentions). */
  name: string;
  /** Full name of the user. */
  real_name: string;
  /** Name shown in the UI. Falls back to real_name if empty. */
  display_name: string;
  /** Email address associated with the user. */
  email: string;
  principal_type: PrincipalType;
  account_type: AccountType;
  /** For agent users, the human user who owns this agent. */
  owner_id?: string;
  /** Administrative roles delegated to this user. */
  delegated_roles?: DelegatedRole[];
  /** Whether this user is a bot/agent (true) or human (false). */
  is_bot: boolean;
  /** Whether this user has been deactivated. */
  deleted: boolean;
  profile: UserProfile;
  created_at: string;
  updated_at: string;
}
