/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * Teraslack API
 * Canonical HTTP API for Teraslack.
 * OpenAPI spec version: 1.0.0
 */

/**
 * A single-use workspace invite scoped to one email address.
 */
export interface WorkspaceInvite {
  /** Unique invite identifier. */
  id: string;
  /** Workspace this invite grants access to. */
  workspace_id: string;
  /** Email address allowed to accept the invite. */
  email: string;
  /** User who created the invite. */
  invited_by: string;
  /** Canonical account that accepted the invite. */
  accepted_by_account_id?: string;
  /** When the invite expires. */
  expires_at: string;
  /** When the invite was accepted. */
  accepted_at?: string;
  created_at: string;
  updated_at: string;
}
