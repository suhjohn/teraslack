/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * Teraslack Messaging API
 * Canonical HTTP API contract for the Teraslack server.
 * OpenAPI spec version: 0.1.0
 */
import type { AgentMode } from './agentMode';
import type { AgentOwnerType } from './agentOwnerType';
import type { User } from './user';

export interface Agent {
  user: User;
  /** Whether the agent is directly owned by a user or by one workspace. */
  owner_type: AgentOwnerType;
  /**
     * Direct owner when owner_type is user.
     * @nullable
     */
  owner_user_id?: string | null;
  /**
     * Owning workspace when owner_type is workspace.
     * @nullable
     */
  owner_workspace_id?: string | null;
  /** Effective write mode for API-key-authenticated agent actions. */
  mode: AgentMode;
  /** User who created the agent. */
  created_by_user_id: string;
  /** Creation time. */
  created_at: string;
  /** Last metadata update time. */
  updated_at: string;
}
