/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * Teraslack API
 * Canonical HTTP API for Teraslack.
 * OpenAPI spec version: 1.0.0
 */
import type { EventSubscriptionResourceType } from './eventSubscriptionResourceType';

/**
 * A webhook subscription that delivers events to an external URL.
 */
export interface EventSubscription {
  /** Unique subscription identifier. */
  id: string;
  /** Workspace this subscription belongs to. */
  workspace_id: string;
  /** HTTPS URL where events are delivered via POST. */
  url: string;
  /** Filter to a specific event type (e.g. "conversation.message.created"). Omit to receive all types. */
  event_type?: string;
  /** Filter events to a specific resource type. */
  resource_type?: EventSubscriptionResourceType;
  /** Filter events to a specific resource instance. */
  resource_id?: string;
  /** Whether this subscription is currently active. */
  enabled: boolean;
  created_at: string;
  updated_at: string;
}
