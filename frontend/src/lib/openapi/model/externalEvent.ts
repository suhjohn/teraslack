/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * Teraslack API
 * Canonical HTTP API for Teraslack.
 * OpenAPI spec version: 1.0.0
 */
import type { ExternalEventPayload } from './externalEventPayload';
import type { ExternalEventResourceType } from './externalEventResourceType';

/**
 * An event representing something that happened in the system (message sent, user joined, etc.).
 */
export interface ExternalEvent {
  /** Monotonically increasing event ID. Use as cursor for pagination. */
  id: number;
  /** Workspace where the event occurred. */
  workspace_id: string;
  /** Event type (e.g. "conversation.message.created", "user.updated"). */
  type: string;
  /** Type of resource that produced the event. */
  resource_type: ExternalEventResourceType;
  /** Identifier of the resource that produced the event. */
  resource_id: string;
  /** When the event occurred. */
  occurred_at: string;
  /** Event-specific data (e.g. the full message object for a message event). */
  payload: ExternalEventPayload;
}
