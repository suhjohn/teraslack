/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * Teraslack API
 * Canonical HTTP API for Teraslack.
 * OpenAPI spec version: 1.0.0
 */
import type { FreeFormObject } from './freeFormObject';

/**
 * A record of an authorization-relevant action (permission change, access grant, etc.).
 */
export interface AuthorizationAuditLog {
  /** Unique audit log entry identifier. */
  id: string;
  /** Workspace where the action occurred. */
  workspace_id: string;
  /** User who performed the action. */
  actor_id?: string;
  /** API key used to perform the action (if applicable). */
  api_key_id?: string;
  /** User the action was performed on behalf of (for delegated actions). */
  on_behalf_of?: string;
  /** Action performed (e.g. "grant_access", "revoke_key"). */
  action: string;
  /** Type of resource affected. */
  resource: string;
  /** Identifier of the resource affected. */
  resource_id: string;
  /** Additional context about the action. */
  metadata?: FreeFormObject;
  created_at: string;
}
