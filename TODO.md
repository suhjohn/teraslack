# TODO

- [x] Write down the final identity model in `docs/` and code comments:
  `Account` is the global auth identity, `User` is the workspace-local identity/access/persona, and `WorkspaceMembership` is removed completely.
- [x] Remove `WorkspaceMembership` as a persisted domain concept from `server/internal/domain/workspace_membership.go`.
- [x] Remove `WorkspaceMembershipRepository` from `server/internal/repository/interfaces.go`.
- [x] Delete `server/internal/repository/postgres/workspace_membership.go` and all SQLC/query plumbing for workspace memberships.
- [x] Remove `workspace_memberships` from the database schema with a new migration after data has been copied into `users`.
- [x] Add `account_id` to `users` if it is not already present in the final schema migration path.
- [x] Add a unique constraint on `(account_id, workspace_id)` for `users`.
- [x] Backfill every existing user with the correct `account_id` from `workspace_memberships`.
- [x] Backfill every auth/session/oauth/account-linked record that still relies on `membership_id` or membership lookups so it resolves directly through `users.account_id` and `users.workspace_id`.
- [x] Remove nullable/legacy membership bridge fields once the backfill is complete.
- [x] Decide whether `users.id` stays as-is or is regenerated during migration; prefer keeping existing IDs stable if possible.
- [x] Make `users.account_type` the canonical workspace authorization field.
- [x] Remove any remaining code that treats membership account type as more canonical than `users.account_type`.
- [x] Make `accounts.email` the only canonical email in the system.
- [x] Remove `users.email` as an authoritative field and make it either mirrored from `accounts.email` or stop storing it entirely.
- [x] Make `accounts.principal_type` the only canonical principal type in the system.
- [x] Remove `users.principal_type` as an authoritative field and make it either mirrored from `accounts.principal_type` or stop storing it entirely.
- [x] Make `accounts.is_bot` the only canonical bot/system flag in the system.
- [x] Remove `users.is_bot` as an authoritative field and make it either mirrored from `accounts.is_bot` or stop storing it entirely.
- [x] Decide whether `deleted` is global on `accounts`, workspace-local on `users`, or intentionally duplicated with one canonical owner.
- [x] Keep workspace-local display/profile/persona data on `users`.
- [x] Refactor `server/internal/service/identity.go` to remove all membership-based logic and make identity resolution strictly `Account -> User in workspace`.
- [x] Delete lazy compatibility user materialization code.
- [x] Delete `GetByLegacyUserID`, `AttachUser`, and other legacy membership-bridge helpers.
- [x] Remove `Compatibility*` naming from the service layer.
- [x] Refactor `server/internal/service/actor.go` so the canonical actor is `account_id + user_id + workspace_id`, not membership-based.
- [x] Refactor `server/internal/service/authz.go` so all workspace authz decisions are made directly from the acting `User` row plus linked `Account`.
- [x] Stop treating `ctxutil.GetMembershipID()` as meaningful in request handling.
- [x] Remove `ContextKeyMembershipID` and all membership-specific context wiring from `server/internal/ctxutil/ctxutil.go`.
- [x] Make request auth context canonical fields be:
  `account_id`, `user_id`, `workspace_id`, `principal_type`, `account_type`, `is_bot`, `permissions`, `scopes`.
- [x] Refactor `server/internal/service/auth.go` so session validation resolves account first and user-in-workspace second, with no membership dependency.
- [x] Remove `membership_id` from the authoritative auth/session model in `server/internal/domain/auth.go`.
- [x] Remove `membership_id` from `AuthContext`, `AuthMeResponse`, `AuthSession`, and `OAuthAccount` once storage and handlers are updated.
- [x] Refactor signup flow so it creates or reuses `Account`, then creates or reuses `User` in the target workspace.
- [x] Refactor OAuth login flow so it creates or reuses `Account`, then creates or reuses `User` in the target workspace.
- [x] Refactor workspace invite acceptance so it creates or reuses `Account`, then creates or reuses `User` in the invited workspace.
- [x] Remove membership-specific audit payloads and replace them with account/user/workspace fields.
- [x] Update `auth_sessions` schema and code to stop carrying `membership_id`.
- [x] Update `oauth_accounts` schema and code to stop carrying `membership_id`.
- [x] Update `workspace_invites` schema and code to stop carrying `accepted_by_membership_id`.
- [x] Remove `accepted_by_membership_id` from OpenAPI and responses.
- [x] Decide whether `workspace_invites.accepted_by_user_id` remains the workspace-local acceptance record or should shift to account-only plus derived user lookup.
- [x] Refactor `server/internal/service/user.go` so `UserService` becomes the canonical workspace-local identity/access service.
- [x] Make `UserService` the owner of workspace-scoped admin/member role updates.
- [x] Keep `/workspaces/{id}/users` as the canonical workspace-local user surface.
- [x] Remove top-level `/users` routes from the public API.
- [x] Update all handlers and router wiring to use workspace-scoped user APIs only.
- [x] Replace all remaining membership-shaped API semantics with account- or user-shaped semantics.
- [x] Make `/me` or `/auth/me` account-first:
  it should return account info globally plus selected/current workspace user info when available.
- [x] Decide whether the final endpoint path is `/me` or `/auth/me` and normalize the whole stack to it.
- [x] Update `server/api/openapi.yaml` to remove `WorkspaceMembership` from the public contract.
- [x] Update `server/api/openapi.yaml` to make account-first identity responses canonical.
- [x] Update `server/api/openapi.yaml` to make workspace-scoped user routes canonical.
- [x] Update `server/api/openapi.yaml` to remove any `membership_id` fields from schemas and request/response bodies.
- [x] Update `server/internal/handler/openapi_server.go` to match the new contract.
- [x] Regenerate `server/internal/api/openapi.gen.go`.
- [x] Regenerate frontend OpenAPI/orval models after the spec changes.
- [x] Update `frontend/src/lib/api.ts` to reflect the final `Account + User` model.
- [x] Update frontend auth state to treat account as canonical and current workspace user as derived workspace context.
- [x] Update frontend workspace user management screens to operate on workspace-scoped users only.
- [x] Update frontend settings/admin flows that currently rely on top-level users or membership semantics.
- [x] Update frontend copy and docs to remove mentions of workspace memberships.
- [x] Keep product/content tables on `user_id` because `User` is the workspace-local actor model.
- [x] Keep `messages.user_id` as the message author reference.
- [x] Keep `reactions.user_id` as the reaction actor reference.
- [x] Keep `files.user_id` as the uploader/owner reference.
- [x] Keep `conversation_members.user_id` as the visible channel membership reference.
- [x] Keep conversation manager and posting policy actor references on `user_id` if those operations are intended to be workspace-user scoped.
- [x] Decide whether delegated roles remain attached to `user_id` or should move to `account_id + workspace_id`; prefer staying on `user_id` if `User` is now the workspace access record.
- [x] Refactor `server/internal/service/role.go` and `server/internal/repository/queries/roles.sql` accordingly.
- [x] Refactor `server/internal/service/conversation_access.go` so role/admin checks read directly from `User` and linked `Account`.
- [x] Refactor `server/internal/service/workspace.go` so primary admin transfer and admin checks operate directly on workspace users.
- [x] Refactor `server/internal/service/external_member.go` so external access checks resolve through `Account` and target workspace `User` when needed.
- [x] Refactor `server/internal/service/search.go` to remove membership-specific visibility logic.
- [x] Refactor `server/internal/service/external_event.go` and event visibility logic to remove membership-specific principal handling.
- [x] Refactor `server/internal/repository/postgres/external_event.go` to stop filtering on `membership_id`.
- [x] Remove membership-specific fields from cursor state and event visibility principal structs.
- [x] Decide whether `ResourceTypeUser` in external events continues to mean workspace-local `User`; likely yes.
- [x] Keep public events product-friendly and user-based, even if auth/account state is account-first internally.
- [x] Update `server/internal/service/authorization_audit.go` so canonical actor metadata is `account_id + user_id + workspace_id`.
- [x] Update authorization audit log payloads and metadata to stop referring to membership IDs.
- [x] Update internal event metadata to carry canonical account/user/workspace actor fields.
- [x] Review `AggregateUser` in `server/internal/domain/internal_event.go` and keep it if `User` remains the workspace-local actor aggregate.
- [x] Remove any aggregate/resource concepts that only existed for membership.
- [x] Refactor API keys to support exactly two scopes:
  account-level keys and workspace/system-level keys.
- [x] Remove user-level API keys from the model and implementation.
- [x] Define account-level API keys as keys owned by an `Account` that can act in any workspace where that account has a `User` by default.
- [x] Add optional `workspace_ids` allowlist support to account-level API keys.
- [x] Define empty or null `workspace_ids` on an account-level API key to mean all workspaces where the account has a `User`.
- [x] Define non-empty `workspace_ids` on an account-level API key to mean only that subset of workspaces is allowed.
- [x] Define workspace/system-level API keys as keys owned by a workspace-scoped system principal that is not tied to a user persona.
- [x] Refactor `server/internal/domain/api_key.go` to express account-level and workspace/system-level ownership clearly.
- [x] Refactor `server/internal/service/api_key.go` so account-level key validation resolves `Account -> User in target workspace`.
- [x] Make account-level API key requests fail when the account has no user in the target workspace.
- [x] Make account-level API key authorization derive from the selected workspace user’s `account_type` and delegated roles.
- [x] Make account-level API key request handling use explicit `workspace_id` when provided.
- [x] Make account-level API key request handling infer the workspace only when exactly one eligible workspace exists.
- [x] Make account-level API key request handling fail as ambiguous when no `workspace_id` is provided and multiple eligible workspaces exist.
- [x] Add schema support for `workspace_ids` restrictions on account-level keys.
- [x] Update API key creation, listing, update, rotation, and validation handlers and OpenAPI models to match the new two-scope model.
- [x] Update frontend API key screens to reflect account-level keys and workspace/system-level keys only.
- [x] Remove all `WorkspaceMembership` references from MCP server code in `server/internal/teraslackmcp/server.go`.
- [x] Remove all `WorkspaceMembership` references from stdio MCP server code in `server/internal/teraslackstdio/server.go`.
- [x] Update `server/internal/service/mcp_oauth.go` so claims encode the final account-first, user-in-workspace model.
- [x] Keep `user_id` in tokens only as the current workspace actor, never as a stand-in for a removed membership concept.
- [x] Remove `workspace_memberships` mentions from README files, docs, and comments.
- [x] Update any docs that describe `User` as a global identity rather than a workspace-local identity.
- [x] Update any docs that describe `Account` as an intermediate compatibility layer rather than the canonical auth identity.
- [x] Update migration notes and operational docs so deploy order and data backfill steps are explicit.
- [x] Add tests that assert every account can have multiple users across workspaces but at most one user per workspace.
- [x] Add tests that assert auth/session flows never rely on membership rows.
- [x] Add tests that assert account-level keys resolve correctly across multiple workspaces.
- [x] Add tests that assert workspace/system-level keys do not require a user.
- [x] Add tests that assert `/me` returns account-first identity plus workspace-local user context.
- [x] Add tests that assert top-level `/users` no longer exists.
- [x] Add tests that assert workspace-scoped user routes are canonical.
- [x] Regenerate SQLC code after query/schema updates and fix compile errors all the way through.
- [x] Run `go test ./...` in `server` and fix all failures.
- [x] Run frontend build/test/lint and fix all failures caused by the API and model changes.
- [x] Remove the final dead runtime code, compatibility helpers, and stale terminology after the new model is fully stable.
  Historical migrations are intentionally preserved as migration history rather than deleted.
